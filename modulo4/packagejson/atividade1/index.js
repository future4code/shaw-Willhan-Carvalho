// -------- Exercício 1 ---------- 
const name = (process.agv[2]);
const age = Number(process.agv[3]);

const info = `"Olá, ${name}! Você tem ${age} anos."`

const newIdade = age + 7

const infoTwo = `Olá, ${name}! Você tem ${age} anos. Em sete anos você terá ${newIdade}.`